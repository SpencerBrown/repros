# config for config server

processManagement:
  fork: true

storage:
  dbPath: ../data/config
  journal:
    enabled: true
  engine: wiredTiger
  wiredTiger:
    engineConfig:
      cacheSizeGB: 0.5

systemLog:
  destination: file
  path: ../data/config.log
  verbosity: 1

net:
  bindIp: 127.0.2.1,192.168.0.42
  port: 27019
  unixDomainSocket:
    enabled: false
  ssl:
    mode: preferSSL
    PEMKeyFile: tls-certs/public-server-key-cert.pem
    CAFile: tls-certs/public-signing-ca-root.pem
    allowConnectionsWithoutCertificates: true

security:
  authorization: enabled
  clusterAuthMode: x509
#  keyFile: tls-certs/server.keyfile

setParameter:
  authenticationMechanisms: SCRAM-SHA-1,SCRAM-SHA-256,PLAIN,MONGODB-X509

replication:
  replSetName: cs

sharding:
  clusterRole: configsvr
